{% extends 'AppBundle::layout.html.twig' %}

{% block title %}Hello{% endblock %}

{% block body %}

    {#
    {{ form(form) }}

    {% if data %}
        Code: <pre><code>{{ data }}</code></pre>
    {% endif %}
    #}

{% endblock %}
