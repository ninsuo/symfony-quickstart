{% extends 'FuzQuickStartBundle::layout.html.twig' %}

{% block body -%}
    <h1>Test</h1>

    <table class="record_properties table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ entity.phone }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
        </tbody>
    </table>

            <a href="{{ path('test_edit', { 'id': entity.id }) }}" class="btn btn-default">
        {{ 'quickstart.crud.edit.button' | trans }}
    </a>
    <div class="clear">&nbsp;</div>
    {{ form(delete_form) }}
<div class="text-center">
    <a href="{{ path('test') }}" class="btn btn-default">
        {{ 'quickstart.crud.back' | trans }}
    </a>
</div>
{% endblock %}
