{% block extends %}
{{ "{% extends 'AppBundle::layout.html.twig' %}" }}
{% endblock extends %}

{% block body %}
{{ "{% block body %}" }}
    <h1>{{ "{{ 'base.crud.edit.title' | trans({'%entity%': '" ~ entity ~ "'}) }}" }}</h1>

    {{ '{{ form_start(edit_form) }}' }}
        {{ '{{ form_widget(edit_form) }}' }}
        <input type="submit" value="{{ "{{ 'base.crud.button.update' | trans }}" }}" class="btn btn-primary"/>
    {{ '{{ form_end(edit_form) }}' }}

{% set hide_edit, hide_delete = true, false %}
{{ include('crud/views/others/record_actions.html.twig.twig') -}}
{{ "{% endblock %}" }}
{% endblock body %}
