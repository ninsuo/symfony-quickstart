{% block extends %}
{{ "{% extends 'AppBundle::layout.html.twig' %}" }}
{% endblock extends %}

{% block body %}

{{ "{% block title %}" ~ "{{ 'base.crud.new.title' | trans({'%entity%': '" ~ entity ~ "'}) }}" ~ "{% endblock %}" }}

{{ "{% block body %}" }}
    {{ '{{ form_start(form) }}' }}
        {{ '{{ form_widget(form) }}' }}
        <input type="submit" value="{{ "{{ 'base.crud.button.create' | trans }}" }}" class="btn btn-primary"/>
    {{ '{{ form_end(form) }}' }}

{% set hide_edit, hide_delete = true, true %}
{{ include('crud/views/others/record_actions.html.twig.twig') -}}
{{ "{% endblock %}" }}
{% endblock body %}
